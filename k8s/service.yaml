apiVersion: v1
kind: Service
metadata:
  name: cloud-native-app-service
  namespace: default
  labels:
    app: cloud-native-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: cloud-native-app
